import styled from 'styled-components'

export const PasswordBlock = styled.div`
  width: 90%;

  margin: auto;
  
  button {
    margin-top: 20px;

    @media (max-width: 425px) {
      position: static;
    }
  }
`
